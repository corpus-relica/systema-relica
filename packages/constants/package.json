{
  "name": "@relica/constants",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/constants.js",
  "types": "dist/constants.d.ts",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "echo \"No linting configured\"",
    "format": "echo \"No formatting configured\"",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
