{
  "name": "@relica/types",
  "version": "0.0.0",
  "description": "Common type definitions for systema-relica",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "echo \"No linting configured\"",
    "format": "echo \"No formatting configured\"",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^4.0.0"
  }
}
