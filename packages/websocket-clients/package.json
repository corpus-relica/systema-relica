{
  "name": "@relica/websocket-clients",
  "version": "1.0.0",
  "description": "Shared WebSocket client implementations for Relica services",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "test": "jest",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@relica/websocket-contracts": "*",
    "socket.io-client": "^4.8.1",
    "socket.io-msgpack-parser": "^3.0.2"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "jest": "^29.0.0",
    "@types/jest": "^29.0.0"
  },
  "peerDependencies": {
    "@nestjs/common": "^10.0.0",
    "@nestjs/config": "^3.0.0"
  }
}